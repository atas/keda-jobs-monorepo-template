apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: keda-jobs-prod

resources:
  - ../../base

labels:
  - pairs:
      app.kubernetes.io/name: keda-jobs
      app.kubernetes.io/env: prod
    includeSelectors: false

# Secrets are managed manually via envsubst
# Run: make apply-secrets (from k8s/ directory)

# Future: Add patches for prod-specific settings
# patches:
#   - path: patches/broker-config.yaml
